using UnityEngine;
using System.Collections;

public class textureController : MonoBehaviour {


	// Use this for initialization
	void Start () {
        Destroy(gameObject, 60);
	}
	
	// Update is called once per frame
	void Update () {
     //   if (transform.GetComponent<customTimer>().ourTimer(60.0f))
     //   {
            
     //   }
	}

}
